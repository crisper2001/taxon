services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      # Polling is often required for file watching to work across file systems (Host -> Docker)
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true