services:
  dev:
    image: node:20-slim
    working_dir: /app
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    command: sh -c "npm install && npm run dev -- --host"
